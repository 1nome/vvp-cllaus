from setuptools import setup, find_packages

setup(
    name='cllaus',
    version='0.1.2',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'pygame'
    ],
    entry_points={

    }
)